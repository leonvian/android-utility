<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
	android:layout_width="fill_parent" android:layout_height="fill_parent"
	android:id="@+id/linear_layout_main"
	android:orientation="vertical" android:background="@android:color/white">
	<EditText android:id="@+id/search_box" android:layout_width="fill_parent"
		android:layout_height="wrap_content" android:hint="Digite aqui para filtrar:"
		android:inputType="text" android:drawableLeft="@drawable/search"
		android:maxLines="1" />
	<ListView android:choiceMode="singleChoice" android:id="@android:id/list"
		android:layout_width="fill_parent" android:layout_height="fill_parent"
		android:dividerHeight="1dp" android:cacheColorHint="#00000000"
		android:divider="#b5b5b5"
		
        android:listSelector="@drawable/list_selector"
		>
	</ListView>
	<TextView android:id="@android:id/empty" android:layout_width="fill_parent"
		android:layout_height="fill_parent" android:gravity="center"
		android:text="[Nenhum registro encontrado" />
</LinearLayout>